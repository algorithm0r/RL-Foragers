{
  "name": "rllayers-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Headless experiment runner for RLLayers (loads the browser sim core via indirect eval; writes self-describing packets to MongoDB via the vendored db.js).",
  "dependencies": {
    "mongodb": "^6.3.0"
  }
}
